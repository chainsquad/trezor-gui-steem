import React, { Component } from 'react';

export default class Login extends Component {

    componentWillMount() {
        
    }

    render() {
        return (
            <div>
                <p>Login should happen here</p>

                <button className="btn btn-success">Connect</button>
            </div>
        );
   }
}
